# Prompt Pack — RailRecruit
Generated by: de-handoff (default mode)
Upstream artifacts: recon.md, growth.md, direction.md, motion.md, master brief
Date: 2026-06-17
Total sections: 6
Signature moment: Outcome Ladder (GSAP ScrollTrigger pinned scrub) — lives in the Outcome Ladder section

---

> Each prompt below is self-contained and copy-paste-ready.
> Hand it to frontend-design or senior-frontend to build the section.
> Do not modify the design tokens or motion spec — they are locked.

**Shared spine tokens (locked — appear in every prompt by reference; do not change):**
```
Colors:  --color-bg-primary #FFFFFF | --color-bg-secondary #EEF2F4 | --color-surface #F7FAFB |
         --color-border #D6DEE3 | --color-text-primary #0E1B2C | --color-text-secondary #3C4A57 |
         --color-text-muted #5B6B7B | --color-accent #0E6E63 | --color-accent-hover #0B574E |
         --color-accent-subtle #E2F0EE
Type:    Geist Sans (400/600/700) + Geist Mono (numerals). Scale: display clamp(2.75rem,6vw,4.5rem)/700,
         h1 2.25rem/700, h2 1.75rem/600, h3 1.375rem/600, body-lg 1.25rem, body 1.0625rem, body-sm 0.875rem,
         ui 0.9375rem/600. Display ≈ 4× body (editorial contrast — not flat).
Spacing: base 8px | section padding 96px desktop / 56px mobile | container max-w 1200px | gutter 24px |
         component gap 32px | inline gap 8px
ONE CTA: "Enroll Now" → /enroll/[course]. Accent-solid, white text, the only primary on the page.
         Secondary actions ("Download syllabus", "Talk to a counsellor") are outline/text — one level below.
```

---

## Hero — Build Prompt

### Context
You are building the Hero section of RailRecruit (CPD-certified railway-signalling training).
This section's purpose: deliver the value prop in < 5 s and route the eye to the single Enroll CTA.

### Design tokens (non-negotiable — do not change)
Use the shared spine tokens above. Background `--color-bg-primary`; ink `--color-text-primary`; CTA `--color-accent`.

### Motion spec (from motion.md)
Motion type: stagger-in (headline → subhead → trust strip → CTA)
Intent: guide the eye down the Z-path to the CTA in the first viewport
Trigger: viewport entry (on load)
Reduced-motion fallback: instant reveal — all elements at final state, no stagger
CLS risk: none (reserve text space; transform/opacity only)
LCP impact: none — headline is LCP; render text first, animate opacity only (never delay LCP)
Technology: CSS (keyframes + IntersectionObserver gate)

### Accountable WOW
Decision: single dominant "Enroll Now" CTA above the fold, value prop + CPD trust strip framing it.
Conversion bet: a clear value prop + single CTA on the scan path beats scattered weak CTAs (recon finding).
Metric it moves: hero enroll-CTA click-through rate.
Experiment: A = multiple equal CTAs vs B = single primary + trust strip. Success: +15% CTA CTR @ 95% conf.
Instrumentation: `enroll_click` (placement=hero), `scroll_50`.
Perf gate: LCP < 2.5 s, CLS = 0 (opacity-only on painted text).
A11y gate: reduced-motion → instant reveal; CTA contrast ≥ 4.5:1; focus-visible ring.

### Copy
Headline: "Become railway-signalling job-ready — CPD-certified."
Subhead: "Training built for diploma & B.Tech graduates and working professionals. Transparent pricing, placement support, money-back on your first module."
Primary CTA: "Enroll Now"  ·  Secondary: "Download syllabus"
Trust strip: "CPD-certified · 9 years training railway-signalling professionals"

### Build instructions
1. Implement at `components/sections/Hero.tsx`.
2. Use only the tokens above — no hardcoded colors, sizes, or spacing.
3. Motion must have a `prefers-reduced-motion` check; fallback = instant reveal.
4. Accessible: semantic `<h1>` for the headline, real `<button>`/`<a>` for CTAs, focus management.
5. Headline is the LCP element — must render in initial HTML; never gate it behind JS.

### Acceptance criteria
- [ ] Design tokens used exclusively (no hardcoded values)
- [ ] Stagger-in implemented; reduced-motion instant-reveal fallback works
- [ ] LCP < 2.5 s, CLS = 0
- [ ] A11y: contrast, focus, semantic h1
- [ ] Exactly one primary CTA above the fold; secondary visually subordinate

---

## Outcome Ladder — Build Prompt  *(SIGNATURE MOMENT)*

### Context
You are building the Outcome Ladder section of RailRecruit.
This section's purpose: make the railway-job outcome feel inevitable — the desire-builder before the decision. **This is the page's ONE signature moment. Do not add a second.**

### Design tokens (non-negotiable — do not change)
Use the shared spine tokens. Section may use `--color-bg-secondary`; step accents `--color-accent`.

### Motion spec (from motion.md)
Motion type: pin-scrub (pinned section; steps advance with scroll progress)
Intent: visualize eligibility → course → CPD certificate → railway role as one inevitable path
Trigger: scroll position (pinned scrub)
Reduced-motion fallback: static stacked four steps, all visible, no pin/scrub — narrative intact
CLS risk: low — reserve pin-spacer height up front; verify no late shift
LCP impact: none — below the fold, deferred
Technology: GSAP ScrollTrigger (+ gsap-timeline). Scrub needs frame-accurate progress binding Framer can't provide.

### Accountable WOW
Decision: a pinned scroll-scrub step sequence dramatizing the path to a railway job.
Conversion bet: raising desire/belief immediately before the pricing block lifts enrollment at the top doubt point.
Metric it moves: scroll-to-CTA rate and enrollment-complete rate.
Experiment: A = static outcome list vs B = pinned scrub ladder. Success: +8% scroll-to-CTA @ 95% conf.
Instrumentation: `ladder_view`, `ladder_complete`, `scroll_to_cta`.
Perf gate: transform/opacity only; pin reserves height (CLS = 0); GSAP+ScrollTrigger ≤ 60 KB gz.
A11y gate: reduced-motion → static stack; pin must NOT trap keyboard focus; linear tab order through steps.

### Copy
Section title: "From where you are to a railway job — step by step."
Steps: 1) "You qualify" (diploma / B.Tech / working pro) → 2) "You train" (CPD-certified signalling course) → 3) "You're certified" (recognised CPD certificate) → 4) "You're hired" (railway-signalling role).

### Build instructions
1. Implement at `components/sections/OutcomeLadder.tsx`.
2. Use only the tokens above.
3. Wrap motion in `gsap.matchMedia()`; under `prefers-reduced-motion` disable pin+scrub → static stack.
4. Accessible: each step a semantic list item; pin is visual only; do not trap focus; release pin on focus-out.
5. Reserve the pin-spacer height to keep CLS = 0.

### Acceptance criteria
- [ ] Design tokens used exclusively
- [ ] Pinned scrub implemented via GSAP ScrollTrigger; reduced-motion static-stack fallback works
- [ ] CLS = 0 (pin reserves height); LCP unaffected (below fold)
- [ ] A11y: no focus trap, linear tab order, steps readable without motion
- [ ] This is the ONLY signature moment on the page

---

## Proof / Testimonials — Build Prompt

### Context
You are building the Proof section of RailRecruit (placement/outcome testimonials).
This section's purpose: deliver trust at doubt moment 1 — "do people like me actually get railway jobs?"

### Design tokens (non-negotiable — do not change)
Use the shared spine tokens. Cards left-aligned (NOT a centred card stack); `--color-surface` card bg, `--color-border` divider.

### Motion spec (from motion.md)
Motion type: stagger-in (cards fade + rise on entry)
Intent: trust delivered exactly at the doubt point
Trigger: viewport entry
Reduced-motion fallback: simple 200 ms fade or instant; static stack
CLS risk: none — media in reserved aspect-ratio boxes (fix for recon's buried-image issue)
LCP impact: none — below fold, images lazy-loaded
Technology: CSS + IntersectionObserver

### Accountable WOW
Decision: relocate buried placement testimonials to a doubt point, with reserved-ratio media.
Conversion bet: proof at peak doubt reduces abandonment for an anxiety-driven audience.
Metric it moves: scroll-to-CTA and enroll-CTA CTR.
Experiment: A = buried testimonials vs B = proof at doubt point. Success: +5% CTA CTR.
Instrumentation: `testimonial_view`, `scroll_to_cta`.
Perf gate: lazy-load below fold; reserved aspect ratios → CLS = 0.
A11y gate: reduced-motion → instant/static; carousel (if any) has accessible controls + reduced-motion fallback.

### Copy
Section title: "They were where you are. Now they work in railway signalling."
Testimonial (verbatim placeholder from recon "what to keep" — confirm against live data): "[Real placement quote — learner name, role, employer]"  ·  Caption: "Placed after completing the CPD course."

### Build instructions
1. Implement at `components/sections/Proof.tsx`.
2. Use only the tokens above; left-aligned cards, no drop-shadow card-stack AI pattern.
3. `prefers-reduced-motion` → static/instant.
4. Accessible: `<figure>`/`<figcaption>` for testimonials; alt text on learner photos; reserved image ratios.
5. Lazy-load media; never cause CLS.

### Acceptance criteria
- [ ] Design tokens used exclusively
- [ ] Stagger-in with reduced-motion fallback
- [ ] CLS = 0 (reserved media ratios)
- [ ] A11y: semantic figure/figcaption, alt text
- [ ] Left-aligned editorial layout (not centred card stack)

---

## Pricing / Decision Block — Build Prompt

### Context
You are building the Pricing / Decision block of RailRecruit.
This section's purpose: remove recon's biggest blocker (no clear pricing) and host the repeated Enroll CTA.

### Design tokens (non-negotiable — do not change)
Use the shared spine tokens. `--color-surface` block bg with a subtle depth (single Concept-3 graft); `--color-accent` CTA.

### Motion spec (from motion.md)
Motion type: static (no scroll motion); CTA hover/focus micro-interaction only
Intent: the decision moment must be calm and instantly legible — motion here adds friction
Trigger: user interaction (CTA hover/focus)
Reduced-motion fallback: color change only, no transform
CLS risk: none — fully static
LCP impact: none — below fold
Technology: CSS

### Accountable WOW
Decision: explicit pricing block — price, inclusions, CPD certification, money-back first module + Enroll CTA.
Conversion bet: transparent pricing + risk-reversal unblocks the ready buyer (recon's top decision-stage blocker).
Metric it moves: course-page → enrollment-complete rate.
Experiment: A = hidden pricing vs B = explicit pricing + risk-reversal. Success: +10% enroll-complete @ 95% conf.
Instrumentation: `pricing_view`, `enroll_click` (placement=pricing), `checkout_start`, `enroll_complete`.
Perf gate: static HTML; LCP < 2.5 s; CLS = 0.
A11y gate: semantic pricing/inclusions list; contrast ≥ 4.5:1; no motion dependency.

### Copy
Title: "One clear price. Everything you need to get certified."
Inclusions: "Full CPD-certified signalling course · graded modules · CPD certificate · placement support."
Risk-reversal: "Money-back guarantee on your first module."
Trust adjacency: "CPD-certified · 9 years · [N] learners trained" (confirm N).
Primary CTA: "Enroll Now"  ·  Secondary: "Talk to a counsellor"

### Build instructions
1. Implement at `components/sections/Pricing.tsx`.
2. Use only the tokens above; subtle surface depth only — no glow, no dark theme.
3. `prefers-reduced-motion` → CTA color-only hover.
4. Accessible: semantic list for inclusions; CTA is real `<a>`/`<button>`; contrast passes.
5. Keep the block fully static (no CLS); trust signals adjacent to price.

### Acceptance criteria
- [ ] Design tokens used exclusively
- [ ] Explicit price + inclusions + CPD + risk-reversal present
- [ ] Static block, CLS = 0, LCP < 2.5 s
- [ ] A11y: semantic list, contrast, focus
- [ ] One primary CTA (matches the locked Enroll Now)

---

## Pre-CTA Reassurance — Build Prompt

### Context
You are building the Pre-CTA reassurance section of RailRecruit (track record + final testimonial).
This section's purpose: trust at doubt moment 2, immediately before the repeated Enroll CTA.

### Design tokens (non-negotiable — do not change)
Use the shared spine tokens. `--color-bg-secondary` fill; `--color-accent-subtle` trust-strip tint.

### Motion spec (from motion.md)
Motion type: stagger-in
Intent: reinforce trust right before the final decision prompt
Trigger: viewport entry
Reduced-motion fallback: instant reveal
CLS risk: none
LCP impact: none — below fold
Technology: CSS + IntersectionObserver

### Accountable WOW
Decision: place institute track-record proof (9 years, learner count) + one final testimonial before the CTA.
Conversion bet: durability/legitimacy proof at the last doubt point lifts final enroll-CTA CTR.
Metric it moves: enroll-CTA click-through (pre-CTA placement).
Experiment: A = no pre-CTA proof vs B = track-record + testimonial. Success: +5% CTA CTR.
Instrumentation: `trackrecord_view`, `enroll_click` (placement=pre_cta).
Perf gate: CLS = 0; below-fold lazy media.
A11y gate: reduced-motion → instant; contrast ≥ 4.5:1.

### Copy
Title: "9 years. Thousands trained. One outcome: railway-ready."
Final testimonial: "[Real outcome quote]"
Closing CTA: "Enroll Now"

### Build instructions
1. Implement at `components/sections/PreCTA.tsx`.
2. Use only the tokens above.
3. `prefers-reduced-motion` → instant reveal.
4. Accessible: semantic headings; alt text; figure/figcaption for the testimonial.
5. No CLS; the closing CTA matches the locked Enroll Now.

### Acceptance criteria
- [ ] Design tokens used exclusively
- [ ] Stagger-in + reduced-motion fallback
- [ ] CLS = 0
- [ ] A11y: semantic structure, contrast, alt text
- [ ] CTA matches the single locked primary CTA

---

## Footer + Mobile Sticky CTA — Build Prompt

### Context
You are building the Footer and the mobile sticky CTA bar of RailRecruit.
This section's purpose: keep the ONE CTA always reachable on mobile + standard footer nav/trust.

### Design tokens (non-negotiable — do not change)
Use the shared spine tokens. Footer `--color-text-primary` on `--color-bg-secondary`; sticky bar `--color-accent` CTA.

### Motion spec (from motion.md)
Motion type: snap (sticky bar slides in after scroll-past-hero); footer static
Intent: ensure the single CTA is always one tap away on mobile
Trigger: scroll position (past hero)
Reduced-motion fallback: sticky bar present statically (no slide)
CLS risk: none — fixed-position overlay, outside document flow
LCP impact: none
Technology: CSS + minimal JS (scroll-threshold class toggle)

### Accountable WOW
Decision: persistent mobile sticky bar carrying "Enroll Now".
Conversion bet: an always-reachable CTA reduces mobile drop-off between doubt points and the decision.
Metric it moves: mobile enroll-CTA CTR.
Experiment: A = no sticky vs B = sticky Enroll bar. Success: +10% mobile CTA CTR.
Instrumentation: `enroll_click` (placement=sticky), `sticky_shown`.
Perf gate: transform translateY only; no CLS (fixed overlay); INP < 200 ms.
A11y gate: keyboard-reachable, focus-visible, contrast ≥ 4.5:1; reduced-motion → no slide.

### Copy
Sticky bar: "Enroll Now" (+ price chip, optional)
Footer: standard nav (Courses, Certification, About, Contact) + "CPD-certified · 9 years" trust line.

### Build instructions
1. Implement at `components/sections/Footer.tsx` (+ `StickyCTA.tsx`).
2. Use only the tokens above.
3. `prefers-reduced-motion` → sticky bar appears without slide.
4. Accessible: sticky CTA in tab order, focus-visible, contrast passes; footer nav uses semantic `<nav>`.
5. Fixed-position overlay must not cover content/focus; no CLS.

### Acceptance criteria
- [ ] Design tokens used exclusively
- [ ] Sticky bar slide-in with reduced-motion static fallback
- [ ] No CLS; transform-only; INP < 200 ms
- [ ] A11y: keyboard-reachable, focus-visible, contrast
- [ ] Sticky CTA matches the single locked primary CTA

---

### Output summary (de-handoff, default mode)
- **Total prompts written:** 6 (Hero, Outcome Ladder, Proof, Pricing, Pre-CTA, Footer+Sticky).
- **Signature moment section:** Outcome Ladder — GSAP ScrollTrigger pinned scrub.
- **CLS/LCP flags requiring resolution before build:** none — Outcome Ladder pin reserves height (CLS-safe); no motion blocks LCP. (3D candidate was cut upstream on the perf floor.)
- **Reminder:** pass `--build` to move from prompts to actual builds (studio loop). This pack is prompts only — no code written.
